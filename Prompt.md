🚀 AI Master Prompt: Build a Production-Grade Containerized IMS SIP PlatformContext & Engineering DirectivesI am building a personal, industrial-production-standard IP Multimedia Subsystem (IMS) SIP Platform deployed on Kubernetes.Core Frameworks: Spring Boot (Java 21, ZGC) wrapping the Jain SIP stack (JSR 32 / NIST reference implementation).Separation of Planes: Java will handle the Signaling Plane (CSCFs) and Control Plane (HSS). A native C engine (Sipwise RTPEngine) will handle the Media Plane (RTP/SRTP streams) directly to completely bypass JVM garbage collection latency loops.Resiliency: Webhooks/REST are prohibited for core processing. We use gRPC for the Cx interface (CSCF ↔ HSS) and Apache Kafka for the charging/billing track (CDF). Resilience4j must guard all inter-process boundaries.Observability: Every Java component must push duplicate SIP messages encapsulated in HEPv3 over UDP to a Homer SIP Capture server, and expose standard Prometheus actuator metrics.Your job is to generate the code, architectural scaffolding, and infrastructure configurations according to the following execution phases. Let's work through this step-by-step.🛠️ Phase 1: Local Kubernetes Environment Setup & Infrastructure BaselineObjectiveEstablish a local multi-node cluster using Kind (Kubernetes in Docker) or Minikube that is explicitly configured to route high-volume layer-4 UDP/TCP traffic.AI TasksKind Configuration: Generate a kind-config.yaml specifying a multi-node cluster (1 control-plane, 2 workers) that maps host ports 5060 (UDP/TCP for SIP) and an explicit NodePort block (e.g., 30000-30100 UDP) straight into the worker nodes for RTP.Ingress Controllers: Provide the helm/manifest deployment steps for ingress-nginx or an Envoy gateway tailored to support L4 UDP/TCP stream forwarding (mapping external incoming port 5060 traffic directly to internal K8s cluster services).Observability Stack: Write the K8s manifests/Helm values file to deploy Homer SIP Capture (Heplify-Server), Prometheus, and Grafana into a dedicated monitoring namespace.🏗️ Phase 2: Project Architecture Scaffolding & Shared LibraryObjectiveCreate a unified Gradle multi-module clean monorepo architecture with a optimized base image configuration.AI TasksDirectory Structure: Define a monorepo setup consisting of:shared-telecom-lib (contains custom Jain SIP utilities, standard HEPv3 encoders, and generic metrics interceptors).services/p-cscf (Spring Boot app).services/i-cscf (Spring Boot app).services/s-cscf (Spring Boot app).services/hss (Spring Boot app).services/cdf-billing (Spring Boot/Java worker).Base Dockerfile Engine: Write a highly optimized, multi-stage Dockerfile utilizing an Eclipse Temurin OpenJDK 21 base. Configure the command execution line (java -jar) to strictly enforce the Z Garbage Collector (ZGC) via flags: -XX:+UseZGC -XX:+ZGenerational.📞 Phase 3: The Signaling Core Engine (Jain SIP + Spring Boot Integration)ObjectiveImplement the low-level Jain SIP lifecycle engine inside a non-blocking Spring Boot component pattern.AI TasksJain SIP Lifecycle Listener: Write a Spring @Configuration and a @Service component that safely instantiates a NIST SipStack and binds a SipProvider to listen on all interfaces over port 5060.NIST Properties Configuration: Provide the explicit Properties map optimized for heavy enterprise loads. Include configurations for thread pooling (gov.nist.javax.sip.THREAD_POOL_SIZE = 64), transaction timeouts, and asynchronous packet parsing.HEPv3 Sidecar Injector: Write a thread-safe helper class within the shared-telecom-lib that catches all incoming/outgoing Jain SIP RequestEvent and ResponseEvent payloads, wraps them in a raw HEPv3 frame structure, and ships them via UDP out to the Homer stack on port 9060.🔀 Phase 4: Core IMS Component Business Logic (P, I, and S-CSCF Engines)ObjectiveProgram the precise 3GPP architectural roles across the isolated signaling layers.AI TasksP-CSCF Logic (The Proxy Network Entrypoint):Implement NAT Traversal handling: Write logic that parses the incoming Via and Contact headers, checks them against the actual packet source IP (Request.getRemoteAddress()), and rewrites the headers to guarantee seamless return paths.Integrate Bucket4j: Implement an IP/SIP URI-based token bucket rate limiter inside the SIP message listener to drop DDoS floods instantly.I-CSCF Logic (The Gatekeeper):Write the gRPC client layer connecting to the HSS to simulate the Cx Interface.Parse inbound registration items, issue a high-speed database location request, and select the correct S-CSCF destination pod.S-CSCF Logic (The Controller State Machine):Implement the Registration Store using an internal in-memory concurrent map or a Redis interface layer.Write the orchestration code that catches a valid INVITE and issues an out-of-band JSON-RPC/UDP control message to RTPEngine to provision audio relay ports for the calling parties.🔒 Phase 5: Resiliency, Control Plane (HSS), & Charging Plane (CDF)ObjectiveHard-wire fault isolation vectors, robust registration management, and a non-blocking decoupled financial accounting stream.AI TasksHSS Service + PostgreSQL: Build the HSS database schema (Users, Identities, Credentials, Active Registrations). Write a clean Spring Data JPA/Hibernate structure running as a K8s StatefulSet.Resilience4j Implementation: Wrap the S-CSCF ↔ HSS gRPC calls inside a Resilience4j Circuit Breaker. If the HSS reaches a 50% timeout threshold, the circuit must trip immediately, causing the S-CSCF listener to execute a fallback method that releases the Jain SIP thread pool and safely returns a SIP/2.0 503 Service Unavailable message.Kafka Event Engine (Charging Plan):Write a Kafka Producer in the S-CSCF that emits structured JSON events (IMS_SESSION_START upon 200 OK, IMS_SESSION_END upon BYE).Write the cdf-billing consumer worker application that reads these logs, runs precise duration calculations, and maps records down into a high-speed analytic repository (ClickHouse).🧪 Phase 6: Validation, Deployment, & Stress SimulationObjectiveDeploy the full platform stack and rigorously validate performance under high concurrency limits.AI TasksUnified Kubernetes Manifest Manifests: Generate complete production-grade K8s configurations:p-cscf-deployment.yaml + internal Service.rtpengine-daemonset.yaml utilizing hostNetwork: true.Strimzi-compatible Kafka manifests.SIPp Verification Testing: Write a complete SIPp XML test scenario script that acts as an automated user terminal agent. The script must execute a standard registration look, issue an INVITE, parse the negotiated media parameters via SDP, hold for 5 seconds, and send a BYE tear-down string.Provide instructions on running a SIPp concurrency test (e.g., 50 calls per second) against the P-CSCF ingress point to verify that the Prometheus dashboards accurately catch performance counters and Homer renders the sequence flow graphs cleanly without packet loss.

sudo snap install microk8s --classic
sudo usermod -a -G microk8s $USER

microk8s enable gpu